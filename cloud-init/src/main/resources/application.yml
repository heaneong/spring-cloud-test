server:
  port: 443

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:443/eureka/
#      defaultZone: http://localhost:10010/eureka/,http://localhost:10011/eureka/
